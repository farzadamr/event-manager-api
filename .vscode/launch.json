{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Backend (Go)",
      "type": "go",
      "request": "launch",
      "mode": "debug",

      "program": "${workspaceFolder}/src/cmd",

      "cwd": "${workspaceFolder}/src",

      "env": {
        "GO_ENV": "development"
      },
      "args": []
    }
  ]
}
